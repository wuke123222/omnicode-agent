# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的格式，并使用语义化版本号。

## [1.10.2] - 2026-08-06

### Fixed

- 修复 A/B 实验室在发布环境中的 Kotlin 编译错误：变体权重、幂等记录窗口、服务实例访问、实验结果回写和 UI 调色板均与当前 API 对齐。
- 将任务实验耗时起点放入实际执行协程，避免启动器局部变量无法跨作用域使用。

## [1.10.1] - 2026-08-06

### Fixed

- 发布构建优先使用 JetBrains 官方 IntelliJ 仓库，并对 Windows Gradle 构建和兼容性验证增加有限重试；短暂的仓库 5xx/网关错误不再直接阻断 Marketplace 发布。
- 发布任务禁用易受外部依赖解析影响的配置缓存，避免缓存序列化错误掩盖真实依赖下载失败。

## [1.10.0] - 2026-08-06

### Added

- 模型目录增加跨 IDE 重启的脱敏最后已知良好缓存；网络失败时保留可用模型并明确提示缓存状态。
- 模型请求显示排队、连接/推理、首 Token 和完成阶段，继续使用有界流式输出。
- MCP 市场增加安装前安全审阅；未签名来源、可变版本、远程凭据和本地可执行文件均在配置前可见，任意 URL/Git 包源拒绝生成草案。
- 统一 JSON 边界拒绝 `null`/数组响应并转换为可诊断的解析错误，避免供应商异常响应直接抛出 `JsonNull` 类型转换错误。

### Fixed

- Windows AppContainer helper 补充标准用户 ACL smoke、系统环境隔离和 Authenticode 发布门禁，避免普通用户运行时误报 fail-closed。
- Remote Robot CI 保存真实 IDE 桌面截图证据，便于审阅缩放、Tool Window 边界和主题回归。

## [1.9.4] - 2026-08-06

### Added

- 活动实验自动接收任务成功率、延迟和 Token 结果，使用 workflow 幂等键避免恢复或重试重复计数。

## [1.9.3] - 2026-08-06

### Fixed

- 实验样本记录增加幂等键，重试不会重复计数。

## [1.9.2] - 2026-08-06

### Added

- 实验卡片增加脱敏样本记录入口，科研来源卡片可直接跳转 MCP 配置页。

## [1.9.1] - 2026-08-06

### Fixed

- 将 A/B 实验测试改为无 IDE 运行时依赖的项目代理，降低发布环境初始化失败风险。

## [1.9.0] - 2026-08-06

### Added

- 新增项目级 A/B Test 实验室：稳定分流、成功率/延迟/Token 计数与有界持久化，不保存提示词或模型输出。
- 新增科研连接器目录：Crossref、OpenAlex、PubMed、arXiv、Semantic Scholar、Science、Nature、知网；授权来源只提供模板，不绕过登录或付费墙。
- 侧栏新增“实验与科研”工作区，集中管理实验和科研来源配置。

## [1.8.1] - 2026-08-06

### Fixed

- 修复 TLS 诊断测试调用对象成员错误，恢复 Windows 发布流水线的完整校验、签名和 Marketplace 发布链路。

## [1.7.3] - 2026-08-05

### Fixed

- 修复篡改许可证在部分 JDK 上抛出底层 `SignatureException`、导致测试失败的问题，统一按无效许可证安全拒绝。

## [1.7.2] - 2026-08-05

### Fixed

- 修复部分 JetBrains IDE 在打开 OmniCode Tool Window 时因无障碍上下文尚未初始化而导致面板空白或初始化失败的问题。

## [1.7.1] - 2026-08-05

### Commercial

- 新增侧边栏“Pro 权益”页面：服务端签发的 Ed25519 许可证在本机验证，token 只进入 IDE Password Safe；非法、篡改或过期 token 自动回到 Free。
- 明确商业边界：现有 Agent/Team、Git Worktree/PR、浏览器工具、跨设备任务包、MCP 和科研附件全部免费，不再由许可证隐藏或降级。
- 新增 Pro 增值模块：项目智能档案（有界脱敏的规则、Harness、上下文和风险摘要）、批量任务配方和工程进展周报（本地 Git 版本差异 + OmniCode 任务账本）；任务可靠性 Markdown 报告保持免费，Research 仅额外提供可选实验锁定信息。权益只在用户主动点击新增产物时校验。

### Chat UX

- 对话输入区新增可切换的“流式 · 开/关”：关闭时回答在任务结束后集中展示，开启时逐段渲染，均保留有界输出。
- 新增固定执行进度条，实时显示当前阶段、耗时、工具、子代理和修改计数；以安全的可审计状态替代模型隐藏思维链展示，减少长任务无反馈感。

## [1.6.4] - 2026-08-05

### Agent

- Team 模式改为单个 Codex App Server 父线程驱动原生协作，由 Codex 自己创建只读子线程，不再为每个角色启动互相独立的伪专家进程。
- 解析原生 `collabAgentToolCall` / `subAgentActivity` 生命周期，进度卡显示真实后端、子线程 ID 和有界处理详情；失败保留实际诊断信息。
- 原生协作统一使用 Codex Ultra 推理并共享任务预算，减少重复启动和上下文串线；主对话仍保留用户选择的供应商。

## [1.6.3] - 2026-08-05

### UI

- 创意工坊主题现在覆盖聊天、侧栏、设置、计划、任务、审阅、MCP 和动态对话卡，不再只改变局部强调色。
- 新增统一主题角色映射，切换主题时同步刷新已有 Swing 控件、滚动区域、卡片边框和状态色。
- 桌宠身体、脸部、描边、状态徽章和阴影与工作台主题联动；虚拟偶像保留可读的肤色，同时混入当前主题。

## [1.6.2] - 2026-08-05

### UI

- Claude Plan 计划审阅卡改为聊天内主视图：步骤编辑区默认展开、取消每步嵌套滚动条，完整看板仅作为高级编辑入口。

## [1.6.1] - 2026-08-05

### UI

- 聊天工作台增加项目/模型/模式/运行状态头部，提供新对话、历史记录和设置快捷入口。
- 侧栏增加“工作台 / 项目配置”分组和 OmniCode 品牌标识；窄工具窗口自动收敛为图标轨道，保留完整悬浮提示与键盘可访问性。
- 头部和侧栏跟随创意工坊主题颜色刷新，不改变 Agent、Plan、MCP、附件或审批行为。
- Codex 原生 App Server 改为 Team/自动路由的只读子智能体后端，不再作为用户可选供应商；主对话继续使用当前配置的模型，Codex 子智能体按需启动且失败时不静默回退。

## [1.6.0] - 2026-08-05

### Added

- 新增可选的“Codex 原生（本机 App Server）”智能体后端。插件通过 Codex 官方本机 JSON-RPC `thread/start` / `turn/start` 接入原生线程、流式回复、推理档位和内置工具循环，不再把 `codex exec` 当作普通命令运行。
- Codex 原生任务沿用 OmniCode 的 Agent / Claude Plan 模式、项目工作目录、审批门和进程沙箱映射；找不到本机 Codex 或 App Server 启动失败时明确失败，不静默回退到其他供应商。
- Provider 配置新增 `codex://local` 地址和无需 API Key 的本机登录说明；模型入口保留当前 Codex 本机配置，避免伪造远程模型列表。

## [1.5.6] - 2026-08-05

### Fixed

- Windows `run_command` 的裸可执行文件解析现在显式补充 `.exe`，保持 argv 直执行并拒绝通过 `.cmd/.bat` 绕过 shell 边界。

## [1.5.5] - 2026-08-05

### Fixed

- Windows CI 的 Claude Plan 只读命令测试改用跨平台 `git status --short`，不再依赖 Unix `ls`。
- 研究包替换审阅在 Windows 文件系统不提供 NIO `fileKey` 时保留创建时间、大小、mtime 和 SHA-256 的竞态校验，不再误拒绝合法导出。

## [1.5.4] - 2026-08-05

### Fixed

- 补齐 Windows Known Folder API 头文件，使 AppContainer profile folder 回退路径在 Windows SDK 10.0.26100 上可编译。

## [1.5.3] - 2026-08-05

### Fixed

- Windows AppContainer helper 在桌面创建的临时 profile 上优先调用系统 profile-folder API，遇到仅接受 package identity 的 runner 版本时按官方布局受控解析 `LocalAppData\Packages\<profile>\AC`，避免误报 probe 失败。

## [1.5.2] - 2026-08-05

### Fixed

- 修复 Windows AppContainer helper 在 Windows SDK 10.0.26100 下的 ACE SID 类型转换、profile folder API 参数和 profile 构造问题，恢复原生 host 的可编译性。

## [1.5.1] - 2026-08-05

### Fixed

- Windows AppContainer 发布构建使用详细 CMake 输出，并兼容 Visual Studio 多配置生成器的实际产物目录；缺少 helper 时会明确失败，避免把路径误判成构建失败。
- Windows native helper 的 SDK 警告不再被误当作编译错误；AppContainer API 探测、工作区 ACL smoke test 和 SHA-256 校验仍是发布前硬门禁。

## [1.5.0] - 2026-08-05

### Added

- 任务中心按 workflow 聚合可靠性事件，显示当前未完成阶段、阶段耗时、模型请求、工具失败、重试次数和最近事件；关闭聊天窗口后仍可定位失败步骤。
- 变更审阅中心的文件与变更块支持安全的 IDE 文件/行号跳转，仍复用项目路径、符号链接和敏感文件边界校验。
- CSV/TSV 附件增加本地有界统计摘要（列、缺失、数值范围、均值和趋势），便于论文和实验数据先做快速核对。
- CSV/TSV 预览增加无第三方依赖的本地数值折线图；扫描型 PDF 在本机安装 Tesseract 时增加有界 OCR 回退，并保留页码标记。
- Team 专家卡片增加“查看处理内容”，保留完整有界的专家证据供主代理审阅，折叠行不再是唯一可见结果。
- 任务中心新增加密 `.omnitask` 导出/导入（PBKDF2 + AES-GCM）；导入生成新的 workflow/run ID 并保持待恢复，二进制附件和凭据不会进入包。
- 新增受审批的 `git_workflow` 与 `browser_automation` 工具：项目范围 Git worktree/`gh pr create`、Playwright 检查/打开/截图，均复用现有 argv、沙箱、超时和审计边界。
- 新增可选 `WorkflowCloudSyncClient`，只向用户自建 HTTPS relay 上传已加密包；新增 `ui-regression.yml` 作为确定性组件截图 CI 门禁。
- `ui-regression.yml` 手动入口接入 IntelliJ Platform Robot Server 和 `RemoteRobotSmokeTest`，在真实 IDE 桌面进程中验证截图链路；普通 PR 不启动长时 IDE。
- Windows 增加原生 AppContainer host：无网络 capability、工作区 ACL 事务、重解析点/超大目录拒绝、异常清理失败关闭；`windows-sandbox.yml` 在 Windows runner 编译并执行真实 smoke。

### Performance

- 冷仓库项目上下文预热增加 1.2 秒首请求软预算；超时先请求模型，后台快照完成后供后续轮次复用，避免大仓库扫描拖住首响应。
- 首轮自动上下文按推理档位采用 24–96 KiB 有界窗口；MCP 首次连接按档位采用 1.5–5 秒软等待，低延迟请求不再被慢服务长时间阻塞。

## [1.4.1] - 2026-08-04

### Fixed

- 完成回复的操作区改为响应式换行布局；在窄 JetBrains 工具窗口中不会再挤出聊天边界。

## [1.4.0] - 2026-08-04

### Added

- 已完成的助手回复现在提供 Codex 风格的“重试”“编辑重试”“任务详情”操作；重试使用创建该回复时捕获的原始任务和附件。

### Safety

- 重试不会静默覆盖输入框中的草稿；附件仍通过既有有界读取、审批、沙箱和检查点边界处理。

## [1.3.0] - 2026-08-04

### Performance

- 项目服务启动时后台预热有界、只读的项目上下文快照，减少大仓库第一次发送任务时的等待；预热不会执行 Harness 命令，也不会改变权限边界。

### Fixed

- 完成行会回填终态 Provider usage；仅在最终响应返回 Token 统计的供应商也能正确显示 Codex 风格用量摘要。

## [1.2.1] - 2026-08-04

### Added

- 完成的助手回复现在显示 Codex 风格的结果指标：总耗时、首个响应耗时、工具调用数和 Token 用量，长任务不再像卡死一样没有反馈。

### Fixed

- 复制回复动作与检查点恢复动作解耦；禁用恢复操作时不会误禁用复制等无关操作，并收窄回归测试选择器。

## [1.2.0] - 2026-08-04

### Added

- Agent 完成文件修改后，聊天流内新增 Codex 风格的变更摘要卡：显示文件数、增删行数，列出可点击的文件/行范围，并可一键进入持久化变更审阅中心。
- 文件引用支持 `path:148-169`、`path 148-169` 和 GitHub 的 `path#L148-L169` 写法；点击引用或聚焦后按 Enter/Space 可在 IDE 中打开对应文件和行范围。
- `list_files` 输出中的项目文件路径现在可直接点击打开，目录和截断提示保持普通文本，减少长列表的视觉噪声。
- 完成的助手回复增加“复制”按钮，复制原始 Markdown/代码文本，不再需要手动拖选长回答。
- 计划模式继续使用窄范围检索和内联审批，不改变 Agent、Plan、Claude Plan、Research 的用户运行边界。

### Fixed

- 文件引用保持工作区相对且 fail-closed，拒绝绝对路径、目录穿越、URL 和非法行范围，避免模型输出污染编辑器跳转。
- 修复未配置 API Key 时本地命令发送按钮仍被禁用的问题。
- 缩短连续对话的首字等待：短时缓存规则、Harness 和固定上下文，并让新任务的初始恢复点与首个模型请求并行；启动恢复点不会覆盖更快写入的运行时快照。
- 长回答改为画布式助手排版，保留工具、变更、审批和恢复卡片；过程步骤和流式文本在 UI 中采用更小的有界窗口，避免 EDT 持续重排。
- 保持 Agent、Plan、Claude Plan 和 Research 的用户自定义运行边界与持续执行语义不变；只优化无副作用的进度展示、上下文缓存和错误提示，不新增 Token、轮次、工具或墙钟硬上限。
- `list_files` 保持原有默认清单规模；目录不存在时会给出窄路径 / `search_text` 的下一步提示，避免模型在无效路径上反复尝试。
- Plan / Claude Plan 提示词现在优先使用窄范围检索，并在证据充分后直接综合，减少无意义的重复探索，不改变可用工具和用户配置的运行边界。
- 修复聊天时间线无法识别全角阶段分隔符（例如 `阶段：context`）的问题。

## [0.14.12] - 2026-08-04

### Fixed

- 修复恢复快照首次读取时重复迁移 legacy 状态、重复脱敏导致的回归测试失败；保留旧状态直到用户明确保存，并让脱敏过程幂等。
- 修正快照压缩测试在文件预算驱逐旧记录时的夹杂因素。

## [0.14.11] - 2026-08-04

### Fixed

- 修复 MCP 并行连接任务未绑定到项目 CoroutineScope 导致 Marketplace CI 编译失败的问题；发布流水线现在可以正常进入打包与兼容性验证阶段。

## [0.14.10] - 2026-08-04

### Changed

- MCP 启动与项目上下文并行执行；离线或未授权 MCP 超过五秒会软失败并先发起模型请求，不再拖住首字响应。
- 可靠性 ledger 改为有界异步落盘；恢复检查点限制为有界消息/观察切片，并在首次读取时压缩旧版本的大快照。
- 工具定义和 Token 估算按 Registry 缓存，供应商请求不再反复深拷贝 MCP schema；流式聊天布局和滚动刷新改为合并调度。
- 使用统计继续嵌入第三方 TokenTracker 本地仪表盘，保留 JCEF 不可用时的外部浏览器兜底。

## [0.14.9] - 2026-08-04

### Changed

- 使用统计页改为直接嵌入第三方 TokenTracker 本地仪表盘，不再展示 OmniCode 自己的 Token、费用和趋势统计；无 JCEF 时保留外部打开兜底。
- TokenTracker 启动入口改为上游推荐的 `npx tokentracker-cli`，仍由用户在终端审阅和执行，OmniCode 不运行 npm/npx。

## [0.14.8] - 2026-08-04

### Changed

- 用量页新增可选的 TokenTracker companion 卡片：检测本机 `tokentracker` CLI 和固定回环面板 `127.0.0.1:7680`，可复制安装/启动命令并在识别成功后打开仪表盘；OmniCode 不执行 npm、远程脚本或未知本地服务，内置用量统计保持独立。
- MCP Streamable HTTP 配置新增经用户确认的 OAuth 自动发现：展示资源/授权 metadata、授权/Token/注册端点与客户端注册能力，仅为空字段填充 Scope；无 Client ID 时优先动态注册，不支持时在打开浏览器前给出一步式配置提示。发现 URL 保持同源/HTTPS、无重定向和有界响应约束，动态客户端凭据继续只存 PasswordSafe。
- Agent 默认持续执行，累计运行时长、轮次和工具调用不再作为任务终止条件；用户可手动停止，也可在运行控制中显式切回有限模式。
- Team 专家继承同一持续执行语义，委派工具不再被主任务墙钟提前截断。
- 持续任务使用有界滚动执行日志，并检测交替重复但观察结果不变的只读工具循环；有限模式的轮次和工具额度改为每次恢复 attempt 重新计算。
- 模型单次输出改为有界分段并可在输出上限或可恢复的流中断后自动续接，避免把超长任务绑定到一次脆弱请求；单个模型响应的工具批次仍有独立安全上限。
- 聊天中的用户与 Agent 回复现在都有清晰圆角边界；Claude Plan 审阅卡把只读模式、可编辑步骤、继续规划和两种执行策略分区展示，并在当前对话内完成审批。
- 桌宠支持工具窗口内拖动与位置记忆，也可切换为不抢焦点的透明桌面浮窗；多屏防出屏、右键停靠/复位/关闭和原创虚拟偶像矢量外观同步完善。

### Fixed

- 修复“全速”任务仍会在 1 小时、128 轮或 256 次工具调用后显示“已达到运行边界”并停止的问题。
- Harness 现在允许持续任务从超过旧有限值的安全检查点继续，同时保留单工具超时、重复动作、连续失败、审批、沙箱与未知副作用保护。
- 旧版本写入的 `BUDGET_EXHAUSTED` 检查点升级后可从任务中心继续；暂停状态不再以红色失败展示，也不会把 Plan 步骤误标为失败。

## [0.14.7] - 2026-07-29

### Changed

- 任务累计输入/输出 Token 与估算费用改为仅统计、不设本地硬上限；旧的有限任务配置会自动迁移，模型上下文、单次输出、供应商账户额度与沙箱审批仍照常生效。
- Team 专家不再分摊主任务 Token 配额或使用更短的 6 轮/8 工具局部额度，改为继承主任务运行保护；委派不再因本地共享预算预检而缩减。

### Fixed

- 运行控制、任务中心与专家卡片不再把循环、时间或上下文边界误称为 Token/费用预算耗尽。
- 修复专家可用墙钟时间长于 `delegate_specialists` 普通工具超时、导致整批被提前取消的问题；协调器现在受父任务墙钟和取消统一约束。
- 上下文窗口溢出和供应商单次输出截断现在提供各自的精简上下文/调整单次输出入口，不再错误引导到已经移除 Token/费用项的运行保护页面。

## [0.14.6] - 2026-07-29

### Changed

- 专家达到预算边界但已形成可用阶段结果时显示“阶段结果”，不再误报为失败；无可用内容时仍明确失败。
- 主代理核验所需的有界机器证据与面向用户的简洁专家摘要分离，并为每位专家至少保留一条短结论，避免先返回的冗长专家挤掉同批结果。

### Fixed

- 修复 `list_files`、嵌套专家 `Evidence` 和未综合目录清单泄漏到最终聊天输出的问题；终止边界仅保留数量、截断状态、结论及少量报告过的文件引用。
- 文件清单识别新增 Windows 分隔符、Unicode、空格与逗号分隔支持，并采用 32 KiB 有界密度扫描，避免把 URL、HTTP/gRPC 版本、语义版本、IP 或正常技术分析误删。

## [0.14.5] - 2026-07-28

### Changed

- MCP 初始化与工具发现改为最多四路有界并发，结果仍按用户配置顺序合并；任务结束或取消时在 IO dispatcher 并行关闭已建立的客户端，避免多个离线服务线性叠加等待时间。
- workflow checkpoint 热路径改为追加式 latest-wins 与周期原子压缩，并使用最多 8 条、合计 8 MiB 的热缓存，减少每个 Provider/工具边界的文件重写和多项目常驻内存。
- 聊天首屏立即显示当前模式进度，流式文本只经过一次合并并保持与工具/状态事件的顺序；底栏改为用户阶段白名单，窄侧栏运行时为重要状态保留整行。
- 超大模型回答跳过 EDT 上的完整富 Markdown 重建，同时继续识别并提供可点击的项目文件与行号引用；用户主动上滚后，排队中的自动滚动不再抢回阅读位置。
- MCP 首次连接审批按顺序显示并可随任务取消关闭；连接失败会保留具体服务器名称和有界错误原因。

### Fixed

- 恢复点、工具审计和用量持久化失败改为持续可见的警告状态，不再伪装成普通完成步骤或被内部 Harness 诊断覆盖。
- 修复多个打开项目交替读取 checkpoint 时重复触发全量压缩，以及工具失败后固定显示“正在调整”的误导文案。

### Security

- 包含“危险工具已开始但结果未知”的恢复证据不会被记录数或文件字节上限静默淘汰；容量不足时在写入前失败关闭并保持原文件不变。
- checkpoint 存储拒绝符号链接，并在实际文件通道打开时使用 `NOFOLLOW_LINKS`；缓存容量按记录数和序列化字节双重限制。

## [0.14.4] - 2026-07-24

### Added

- 侧栏 MCP 配置新增搜索式市场对话框，以卡片列表展示内置精选的参考实现、安装方式、来源链接、权限风险和已配置状态；添加后只生成默认停用的本地草稿。
- 模型回复和工具结果中的项目相对文件引用现在可点击跳转，支持 `path:line`、`path:start-end`、GitHub `#L` 形式及中英文范围连接符；打开范围时会选中对应代码，只有文件名时仅在 IDE 索引结果唯一时跳转。
- Plan / Claude Plan 完成后在聊天流内显示可编辑审批卡，支持继续规划、勾选步骤、逐步执行、自动执行或打开完整看板，批准前保持只读。

### Changed

- MCP 市场扩充到 27 个内置精选参考配置，覆盖开发、数据、科研、效率、浏览器与云基础设施六类，并支持分类筛选；浏览、搜索、选择和创建草稿不访问远程注册表、不下载包、不运行命令也不写入凭据，保存、启用、首次连接、进程沙箱和逐工具审批继续复用原有边界。
- MCP 市场可在打开后异步读取官方 MCP Registry 的只读元数据，以游标分页加载至少 500 个最新版服务器并优先排序开发与科研条目；一小时内存缓存、手动刷新、超时/大小/页数上限和 27 个离线精选回退避免阻塞设置页。Registry 条目明确标为未审阅，浏览不会下载或执行包，添加仍只创建默认停用草稿。
- Team 委派提升为每批最多 4 路并发、单次任务最多 8 个专家；共享预算不足时自动缩减为可运行前缀并把其余目标交回主智能体，专家采用更短的局部轮次和时间边界以降低尾部延迟。
- 项目 Harness 改为面向新手的“项目上下文”入口：首屏给出是否可直接使用及推荐下一步，高级成熟度、精确 argv 与安全边界默认折叠；配置示例仅复制到剪贴板且不会执行命令。
- Agent 可在同一模型轮次请求小批独立只读工具，文件列表默认限制为 160 项并引导缩小范围，减少大仓库输出噪声与额外模型往返。

## [0.14.3] - 2026-07-22

### Added

- 底部“任务 / 子代理 / 编辑”改为真实导航：可打开任务中心、定位最近子代理执行卡、定位编辑或打开任务级变更审阅，并支持鼠标、Enter、Space 与左右方向键。
- 放弃恢复检查点成功后提供 8 秒撤销入口；短时撤销令牌仅保存在内存中，过期后失效且不会覆盖同 ID 的较新检查点。

### Changed

- 输入区工具栏在 200 / 240 / 280 px 窄侧栏中自动切换双行布局，保留附件、模式、Team、项目上下文及发送控件，不再相互覆盖或越界。
- Marketplace 发布改为受保护的 `v*` tag 流程：版本一致性、测试、打包、IntelliJ IDEA/PyCharm/WebStorm 验证矩阵、开发者签名和签名校验全部通过后才允许上传。
- 大仓库 PSI/索引检索迁移到跨 2025.3–2026.2 稳定的 Read Action 入口并保留显式取消点，移除两处废弃或实验 API 调用。

### Fixed

- “放弃检查点”现在会先显示副作用警告，删除请求期间禁用重复操作；删除失败会恢复原操作，不再先清掉恢复入口。

### Security

- 签名证书、私钥、私钥密码和 Marketplace Token 仅通过受保护 GitHub environment secrets 注入发布步骤；普通 push/PR 无需凭据且不会输出 Token。

## [0.14.2] - 2026-07-22

### Added

- 引入正式 Agent Harness：主智能体与专家在 Provider I/O 前冻结运行规格并预检有效工具面、恢复降级和策略摘要；未知副作用状态只向模型暴露非危险工具。
- 侧栏“项目上下文”升级为“项目 Harness”，展示启发式成熟度、知识地图、argv 反馈回路、运行时边界和修复建议，并保留规则、固定/排除文件与 PSI 搜索。
- 新增只读 `inspect_project_harness` 工具与可选 `.omnicode/harness.json`，自动发现常见构建、测试、质量和 CI 证据；刷新和发现绝不执行命令。

### Fixed

- 获批危险工具在局部执行超时或抛出异常后会立即停止任务，写入 `SIDE_EFFECT_STATE_UNKNOWN`，并保留 `executionStarted` 恢复检查点；同批后续工具不会继续产生副作用。
- Provider 单轮返回多个工具调用时改为整批预检后按顺序逐项执行，每项独立计数、审批、审计和 checkpoint；审批拒绝会中止后续调用，调用预算不足则整批零执行。
- 工具 observation 字符预算改为整批共享；预算、重复动作、截断响应和异常调用 ID 都会生成完整审计并保持可重放的 Provider 历史。
- 配置单次任务费用上限后，主模型、视觉辅助模型或专家模型缺少有效定价时会在 Provider 网络请求前明确阻断，并直接打开侧栏价格配置。
- 恢复任务使用包含在途请求的持久化费用基线，只对新增用量计价；旧检查点缺少可信费用基线时不会猜测历史成本。

### Security

- Tool Registry 拒绝空名与重名，Harness 预检在 AgentEngine 主 Provider 请求前拒绝 Registry/limits/identity 绑定不一致及有效工具面中未标记危险的非只读工具；Harness 配置拒绝 shell/内联解释器、绝对路径、符号链接、超限或错误类型，并服从统一 AI ignore 失败关闭策略。
- 费用硬上限不再把未知定价视为可继续；NaN、Infinity、负数和全零价格均不可用于执行边界。
- 批量工具执行保持逐工具审批，且第二项开始前的 checkpoint 包含前项结果，避免中断恢复后重复副作用。
- 未解除的未知副作用不会被普通读取或模型完成响应清除；只要恢复检查点仍被保留，就会阻止同项目其他任务启动新的危险工具，危险工具绕过审批契约也会失败关闭。

## [0.14.1] - 2026-07-22

### Added

- Claude Plan 新增 `/plan <任务>` 单轮入口与输入框 `Shift+Tab` 快速切换；计划可继续完善、直接编辑，并选择手动逐步确认或批准后切换 Agent 连续执行。
- Claude Plan 允许 `rg`、只读 Git 查询及其他经过严格 argv 校验的本地探索命令。

### Security

- Claude Plan 命令除只读 allowlist 外，还强制使用无网络、工作区只读的 OS 沙箱；未知、复合、可写或可扩展执行的命令失败关闭，不能通过审批升级为写权限。

## [0.14.0] - 2026-07-21

### Added

- 新增真正可编辑的 Plan → Agent 看板：步骤可单独批准、跳过、暂停和重试，Agent 每次只执行一个已批准步骤；继续规划会保留已完成与已跳过状态。
- 新增独立 `Claude Plan` 模式：只暴露 IDE 读取、PSI/符号索引与关键词检索工具，禁止命令、文件修改和 MCP；计划获批后再切换 Agent。
- 新增统一任务与历史中心，集中显示运行、待批准、暂停、待恢复、失败、预算终止和完成任务，支持继续、重试、复制及按 workflow 回到安全检查点。
- 新增任务级变更审阅中心，对 `apply_patch` / `apply_change` 直接修改提供逐文件、逐块保留或哈希保护的回退，以及全部已记录修改回退。
- 新增 `AGENTS.md`、`CLAUDE.md`、`.omnicode/rules/*.md` 项目规则，展示本轮实际注入的规则和固定文件；支持固定/排除路径、PSI/符号索引和关键词搜索。
- 新增一键连接诊断与脱敏 ZIP 导出，覆盖凭据存在性、Provider Base URL、JVM 代理、DNS/TLS/HTTP、本地模型能力推测、视觉辅助、MCP OAuth 状态和进程沙箱能力。

### Changed

- 自动项目上下文改为 transient 专用消息块，位于当前用户请求之前，不写入历史、checkpoint 或研究包；按模型上下文窗口、累计输入余量、首个目标和当前目标动态裁剪。
- 项目上下文占用显示改为实际注入的项目规则/固定文件 Token 与模型 context 字符预算，不再混用 workflow 累计 Token 上限。
- 默认聊天工具栏始终显示项目上下文入口；侧栏新增任务中心、计划看板、变更审阅、项目上下文和连接诊断。
- 图片依赖任务在重试前要求重新附加并确认原图；“回到检查点”按所选 workflow 恢复文本状态且绝不自动回放副作用。

### Security

- `.gitignore`、`.aiignore`、`.omnicodeignore`、用户显式排除与敏感凭据/私钥路径通过统一 fail-closed 策略约束规则加载、Pinned Context、PSI 搜索及模型可见的列表、读取、搜索和直接修改工具。
- Claude Plan 的只读边界由 Tool Registry 和执行查找双重强制，不依赖提示词或命令名称白名单。
- 审阅写入与 Agent 启动使用项目级互斥门禁，连续点击和并发任务不能交错修改审阅账本。
- 诊断网络探测不携带 API Key、不跟随重定向；导出只含脱敏 Markdown/JSON，并以新建、所有者可读写的 ZIP 发布。

## [0.13.1] - 2026-07-21

### Fixed

- Team 专家启动前会原子预检每位专家的完整 Token 与费用配额；单轮输出采用分段预算，临近上限时停止调用工具并输出带证据的阶段报告，避免并发专家互相挤占额度后空结果退出。
- Provider 请求在发送前持久化 attempt、预留量与稳定幂等键；网络结果不确定时保守计费，明确未发送或 4xx 时释放预留，IDE 中断恢复后不会漏算或重复折算。
- workflow 共享预算与专家 checkpoint 改为原子更新，避免并发专家覆盖主智能体的新记录。
- 恢复流程只会使用恢复提示后新加入且明确确认的图片，不会发送或删除输入框里原有的草稿附件。
- 代码差异审批默认并聚焦“拒绝”，按 Enter 不会误应用修改。
- 修复视觉辅助模型绕开共享预算持久化的问题，并以实际图片内容生成请求幂等键。

### Security

- MCP HTTP 超时或取消会终止当前客户端并关闭响应流，禁止旧请求与后续请求重叠执行。
- MCP stdio 日志在截断前移除完整注入凭据、短密钥和多行 PEM 私钥；脱敏器异常时失败关闭。
- 用户配置的模型 Base URL 拒绝 user-info、查询参数和 fragment，避免把 API Key 等秘密持久化到 URL。

## [0.13.0] - 2026-07-21

### Added

- 主智能体运行中在本机写入有界、脱敏的 workflow checkpoint；IDE 重启后会把未完成记录标记为中断，并在聊天中提供明确的“继续任务”或“放弃检查点”。
- 新增认证、权限、限流、网络/超时、模型能力、预算、沙箱、配置、取消和未知错误分类，并提供检查配置、切换模型、调整预算、打开沙箱或编辑重试等对应入口。

### Changed

- 上下文压缩现在同时保护系统约束、最初用户目标、当前运行的用户目标/验收条件/约束和最新消息；单个历史组超限时会继续尝试可容纳的其他组。
- Agent 在自身捕获的预算、取消和失败边界输出确定性的“已完成 / 证据 / 剩余 / 风险”部分结果，不为终止摘要额外调用模型或工具。
- OpenAI Chat/Responses、Gemini、Bedrock 与 MCP 客户端对流式或 JSON-RPC 响应中的 `null`、标量及无效数组项进行容错或返回明确协议错误，不再把 Gson 类型转换异常泄漏为 Agent 中止。
- 恢复图片只使用并消费检查点明确要求的数量；要求为零时不触碰当前草稿附件，草稿合并也不会超过附件上限或抢占仍在读取的文件。
- 插件版本升级为 `0.13.0`。

### Security

- checkpoint 中待执行或执行状态不确定的工具不会在恢复时自动重放；恢复先核对当前项目状态，模型再次提出副作用时仍走新的审批和工具校验。
- 危险工具在获批后、真正执行前必须先持久化 `executionStarted` 检查点；写入失败会阻止工具执行，等待审批期间取消不会被误报为副作用状态未知。
- checkpoint 仅保存有界的文本快照、工具观察、预算和执行元数据，并在落盘前脱敏；图片及其他附件二进制不会写入 checkpoint。
- 恢复只覆盖当前设备上的 lead workflow，不会启动无人值守后台任务，也不会恢复跨设备状态或重建完整多智能体任务板。

## [0.12.0] - 2026-07-21

### Added

- 新增按 Provider/模型动态筛选的推理强度：自动、关闭、最低、低、中、高、超高和全速；聊天底栏与侧栏供应商配置均可选择。
- 新增“全速项目预设”，经确认后把 workflow 累计输入/输出预算各提高到 `10,000,000,000` Token，并放宽到 128 轮、256 次工具调用和 1 小时运行时间。

### Changed

- OpenAI Responses/Chat、OpenRouter、Anthropic Messages、Gemini 2.5/3 和 Bedrock Claude/Nova 2 在已验证模型上使用各自原生推理字段；未知兼容模型改用不发送额外 wire 参数的 Agent 执行强度。
- 高推理档位同步提高单轮输出下限和 Provider 请求超时；视觉辅助与 Commit AI 保持 `Auto`，不会继承主模型全速设置。
- Gemini usage 计入思考 Token，Anthropic/Bedrock 工具续轮保留供应商签名的思考块。
- 插件版本升级为 `0.12.0`，MCP client info、Provider HTTP 与 OAuth User-Agent 同步更新。

### Security

- 百亿 Token 仅是 workflow 累计硬预算，不绕过供应商单次限制、费用上限、工具审批或进程沙箱；启用全速与百亿预设均需显式确认。
- 未知或无法验证推理协议的模型不发送猜测字段；仅开放可由本地 Agent 安全实现的低/中/高/全速，原生语义不可模拟的档位会隐藏。

## [0.11.0] - 2026-07-21

### Added

- 新增原创虚拟主唱 Lumi 与吉他手 Aster，使用宿主矢量绘制并响应待命、思考、工具、完成和失败状态。
- 创意工坊新增本地 PNG/JPG 虚拟偶像立绘导入、替换、移除，以及五种状态即时预览。

### Changed

- 插件版本升级为 `0.11.0`，MCP client info、Provider HTTP 与 OAuth User-Agent 同步更新。
- 自定义立绘最长边统一缩至 512 像素，加载后由内存缓存复用，避免工作台刷新重复读盘。

### Security

- 不捆绑或宣传未经授权的第三方角色素材；用户导入前必须确认拥有版权或使用授权。
- 导入图片限制为 8 MB、32–2048 像素且最多 419 万像素的真实 PNG/JPEG；拒绝符号链接、GIF、SVG 和伪格式。
- 图片在后台解码到全新 ARGB 缓冲区，剥离元数据并重新编码为固定位置 PNG；源路径不持久化、不进入项目、模型或 MCP，原子替换失败会保留旧立绘。

## [0.10.0] - 2026-07-21

### Added

- 新增独立 `Team` 执行策略：主智能体可并行委派 Explorer、Planner、Reviewer 完成代码事实调查、实施规划和风险评审。
- 新增 Team 进度卡、每个专家的状态/摘要/Token 展示，以及历史会话策略恢复。
- 新增 workflow 共享 Token/费用账本、聚合用量记录和按 agent ID 隔离的工具审计。

### Changed

- 插件版本升级为 `0.10.0`，MCP client info、Provider HTTP 与 OAuth User-Agent 同步更新。
- Agent / Plan / Research 与 Single / Team 正交组合；一次运行最多并行 2 个专家、共 2 轮 4 个专家。
- 视觉辅助模型也进入同一 workflow 用量账本；供应商缺少 usage 时会估算文本与结构化工具调用的完整输出。

### Security

- 专家使用独立空历史与新的 Provider/AgentEngine，只收到有界原始目标和自己的任务；不共享主智能体或兄弟专家上下文。
- 专家固定为 Plan 权限，只能使用只读项目/Skill 工具，不能写文件、运行命令、连接 MCP、发起审批或递归委派；所有副作用仍只由主智能体执行。
- 并发模型请求先预留共享预算，取消会传播至全部专家；专家事件和返回摘要均有长度上限。
- 共享账本分别执行输入、输出、总 Token 与费用硬限制，避免并发专家绕过单次运行预算。

## [0.9.0] - 2026-07-21

### Added

- 新增侧边栏顶层“创意工坊”，提供 JetBrains Native、Graphite Night、Aurora Night、Forest Terminal 和 Paper Studio 工作台皮肤。
- 新增 Pixel Cat、Code Owl、Rubber Duck 和 Tiny Robot 动画桌宠，并与 Agent 思考、工具调用、完成和失败状态联动。

### Changed

- 插件版本升级为 `0.9.0`，MCP client info、Provider HTTP 与 OAuth User-Agent 同步更新。

### Security

- 创意工坊目录仅接受编译期纯数据，拒绝未知 ID、标记文本、路径与非标准颜色；桌宠包不能注册脚本、命令、类、URL 或反射入口。
- 损坏或已移除的桌宠 ID 会回退到默认项并自动禁用，避免持久化状态意外启用新行为。

## [0.8.0] - 2026-07-19

### Added

- 新增 `Research` 模式：按“研究问题、假设、方法、证据、结果、局限、复现清单、引用”组织研究报告，并要求区分直接观察与推断；不得编造论文、作者、DOI、URL、测量值或实验结果。
- 支持本地有界提取 PDF 论文文本（最大 10 MB、300 页、48,000 字符）和 Jupyter Notebook 的 Markdown/代码 cell（最大 2 MB、200 个 cell、48,000 字符）；Notebook 输出、附件和元数据不会注入上下文。
- 科研附件补充 LaTeX、BibTeX、R、Julia 和 MATLAB 等安全文本格式；扫描型 PDF 可改为上传关键页面截图并使用视觉辅助模型。
- 聊天输入框支持输入 `@` 搜索项目内受支持文件，并通过与拖拽/文件选择相同的附件校验链加入任务。
- 新增可复现实验研究包导出：生成有界、脱敏的 Markdown，记录模式、供应商、模型、研究问题、会话、工具/命令证据、复现清单、局限和引用核对清单。
- 科研空状态卡片会原子切换到 `Research` 再填入任务；`@`/`!` 弹层支持方向键、Enter/Tab 与 Esc，附件托盘在窄窗口中改为单行横向滚动。

### Changed

- 模式选择扩展为 `Agent` / `Plan` / `Research`；用量趋势、工具审计、会话持久化和助手消息标签均记录 Research。
- 插件、MCP client info、Provider HTTP 和 MCP OAuth 的运行时版本标识统一为 `0.8.0`。

### Security

- Research 仅暴露 `READ_ONLY` 与 `COMMAND` 工具；文件修改、未知/第三方外部工具和 MCP 均 fail closed。命令仍逐次审批，并保留 argv 直执行、超时、输出边界、环境清理和所选进程沙箱。
- PDF 使用内存型、有页数/字符上限的本地解析；加密、损坏、超限或无可读文本的 PDF 会被拒绝，不会静默上传原始文档到辅助模型。
- Notebook 使用严格 UTF-8 JSON 流式解析，只保留有界的 Markdown/代码 cell，并拒绝 NUL 或过多控制字符。
- 研究包在任何统计和脱敏前彻底排除 SYSTEM 消息；后台只读取已配置的供应商/MCP 凭据作为内存脱敏字典，凭据本身不会写入导出。自由文本具有脱敏前单块/总输入预算，图片 base64 被省略。
- 新建导出使用无覆盖发布；覆盖已有文件必须确认并绑定目标的 NOFOLLOW 文件身份，写入前若路径、类型、fileKey、大小或修改时间发生变化会 fail closed。

### Licensing

- 引入 Apache PDFBox 3.0.8（Apache License 2.0）用于本地 PDF 文本提取；组件来源和许可声明见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。

## [0.7.0] - 2026-07-19

### Added

- MCP OAuth 2.1：401/403 challenge、RFC 9728/RFC 8414/OIDC 发现、PKCE S256、state、resource audience、动态客户端注册和 Token 刷新。
- MCP 侧边栏新增无认证/Bearer/OAuth 模式、OAuth 登录与退出。
- 图片附件安全缩略图与尺寸信息，Markdown、源码、日志和文本附件支持本地有界预览。
- 模型列表支持搜索、显示全部和用途标签，仍可手动输入任意 Model ID。

### Changed

- 默认隐藏明确用于 Embedding、图片、音频、Realtime、审核、视频、重排序和 OCR 的非对话模型，未知新模型保守保留。
- 模型发现请求相互隔离，刷新失效后必定恢复 UI；空目录仍保留当前模型和手动 Model ID 入口。
- 剪贴板图片采用单并发、有界 PNG 编码和 800 万像素上限；文本预览严格限制扫描范围。
- MCP/OAuth 网络请求共享 Provider 代理策略。

### Security

- OAuth 回调仅监听 `127.0.0.1`，使用随机路径、一次性 state、严格 Host 校验、3 分钟超时和无缓存/CSP 响应。
- Access/Refresh Token 与动态客户端密钥仅存 PasswordSafe，OAuth 错误不回显验证码、密钥或 Token。
- OAuth 会话绑定已保存的 Endpoint、认证模式、Client ID 与 Scope；跨连接刷新单飞，退出登录或永久授权失败后在途结果不能恢复旧凭据。
- 删除 MCP Server 只在配置成功保存后清理 PasswordSafe，取消或重置不会误删凭据。
- 远程 MCP 不允许通过 challenge 将 resource metadata 发现重定向本机 loopback；loopback 请求永不经过代理。

## [0.6.0] - 2026-07-19

### Added

- MCP 2025-11-25 Streamable HTTP：JSON/SSE、Session 恢复、PasswordSafe Bearer Token、连接审批和安全工具发现。
- 侧边栏“运行控制”：可配置 Agent 轮次、工具、时间、Token、Provider 重试和单次费用上限。
- Linux `workspace-write` 新增经过文件与网络能力探测的 bubblewrap 后端，提供私有 HOME/tmp、只读宿主运行时与默认断网。
- 支持拖拽上传 Markdown、文本、日志、数据和常见源码文件，并可直接粘贴剪贴板截图。
- 贡献规范、安全漏洞报告政策和持续集成构建检查。

### Changed

- Provider 对 429、5xx 和网络故障支持有界指数退避与 `Retry-After`，流式输出开始后不自动重放。
- 沙箱页新增本机能力检测和跨平台安装指引。

### Security

- Windows 宿主在没有可证明的 AppContainer/WSL 路径边界时保持 fail closed，并提供 WSL Remote Development 迁移指引。
- 远程 MCP 强制 HTTPS（loopback 除外）、拒绝重定向并对返回文本中的 Bearer Token 脱敏。
- 危险工具的审批结果必须在执行前成功持久化；审计失败时拒绝执行。

## [0.5.0] - 2026-07-19

### Added

- 精确上下文 Patch 工具，支持审批前后哈希校验和单次 IDE Undo。
- Plan 完成后可确认并一键切换 Agent 执行。
- 编辑器与项目树右键菜单可将当前文件或选区发送到 OmniCode。
- 失败、取消及预算耗尽任务支持 checkpoint 和“编辑后重发”。

### Security

- API 凭据按供应商与 Origin 隔离，远程地址强制 HTTPS，并禁止自动重定向。
- MCP 启动新增项目级配置指纹、显式审批、持久信任撤销和工具审计。

## [0.4.0]

当前开发版本。功能概览和已知限制见 [README.md](README.md)。
