# OmniCode UI fixture

This bounded project exists only so the Remote Robot workflow can open a real IntelliJ project
and exercise the OmniCode Tool Window. It contains no credentials, build scripts, or executable
Harness configuration.
